{
  "name": "@nimbalyst-community/extension-onedev-importer",
  "version": "0.1.0",
  "description": "OneDev issues importer for the Nimbalyst tracker",
  "main": "dist/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build && vite build --config vite.backend.config.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.1.12",
    "vitest": "^3.0.0"
  }
}
